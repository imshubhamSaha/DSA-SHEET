/**
 * @param {number} N
 */
class Solution {
    printPattern(n) {
        let str = "";
        for(let i = 0; i < n; i++) {
            for (let j = 0; j <= i; j++) {
                str += "*";
            }
            str += " ";
        }
        
        console.log(str);
    }
}
