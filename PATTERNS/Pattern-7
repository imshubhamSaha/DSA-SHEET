class Solution {
/**
* @param number n

* @returns none
*/
    printTriangle(n) {
        for (let i = 1; i <= n; i++) {
            const str = Array.from({length:i}, (_, i) => (i+1) + " ");
            console.log(str.join(""));
        }
    }
}
        
