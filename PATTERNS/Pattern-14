const makeBarfi = function(n) {
  let result = "";
  for (let i = 1; i <= ((2 * n) - 1); i++) {
    let stars = i > n ? (2 * n) - i : i; 
    
    for (let j = 0; j <= stars; j++) result = result + "*";
    result = result + "\n";
  }

  return result;
}

console.log(makeBarfi(3))
